open ReasonUrql;
/* Be sure to open the Hooks module to bring necessary types into scope. */
open Hooks;

[@react.component]
let make = _ => {
  
  <div>
    <button >
        "Execute Mutation"->React.string
    </button>
  </div>
}
